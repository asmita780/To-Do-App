# To-Do-App
This is flask To-Do app, in this app python with flask framework , html, and css is used. App will allow the user to set routing for today task will done in three steps if task is not started status of task will be pending, if task is started status of that task will be converted into working and at the end if task will finish status sets as done.
